# Weather Forecast Application

A full-stack weather application with a Node.js/Express backend and React/Vite frontend.

## Structure
- `client/`: React frontend
- `server/`: Node.js backend

## Getting Started
See `walkthrough.md` in the artifacts folder, or check individual READMEs in `client` and `server`.
